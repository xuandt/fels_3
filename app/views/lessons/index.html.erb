<% provide(:title, 'Lessons') %>
<h1>Lessons</h1>

<%= will_paginate %>
<ul class="lessons">
<% @lessons.each do |lesson| %>
    <li>  
      <%= link_to lesson.id, lesson %>  
      . <%= link_to lesson.category_id, lesson %>
    </li>
  <% end %>
</ul>
<%= will_paginate %>