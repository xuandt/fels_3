<%= form_tag(words_path, :method => "get", id: "search-form") do |f| %>
  <table>
    <tr>
      <td>
        Category <%= select_tag 'category_id', options_from_collection_for_select(Category.all, :id, :name, params[:category_id]), include_blank: true %>
      </td>
      <td><%= submit_tag "Search", :name => nil %></td>
    </tr>
  </table>
<% end %>

